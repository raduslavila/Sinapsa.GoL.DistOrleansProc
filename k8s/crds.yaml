# Orleans.Clustering.Kubernetes CRDs
# Apply once per cluster before deploying silos:
#   kubectl apply -f k8s/crds.yaml
#
# Source: https://github.com/OrleansContrib/Orleans.Clustering.Kubernetes
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusterversions.orleans.dot.net
spec:
  group: orleans.dot.net
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            clusterId:
              type: string
            clusterVersion:
              type: integer
  scope: Namespaced
  names:
    plural: clusterversions
    singular: clusterversion
    kind: OrleansClusterVersion
    shortNames:
      - ocv
      - oc
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: silos.orleans.dot.net
spec:
  group: orleans.dot.net
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          required: [address, port, generation, hostname, status, siloName]
          type: object
          properties:
            clusterId:
              type: string
            address:
              type: string
            port:
              type: integer
            generation:
              type: integer
            hostname:
              type: string
            status:
              type: string
            proxyPort:
              type: integer
            siloName:
              type: string
            suspectingSilos:
              type: array
              items:
                type: string
            suspectingTimes:
              type: array
              items:
                type: string
            startTime:
              type: string
            iAmAliveTime:
              type: string
  scope: Namespaced
  names:
    plural: silos
    singular: silo
    kind: OrleansSilo
    shortNames:
      - oso
      - os
